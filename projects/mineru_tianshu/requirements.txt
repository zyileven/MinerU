# MinerU Tianshu Requirements
# 天枢项目依赖

# 关键依赖版本约束 (帮助 pip 更快解析依赖)
# 这些包被 vllm/mineru 依赖，先明确版本避免冲突
einops>=0.8.0  # einops 0.1.0 太旧，导致依赖解析失败
torch>=2.0.0  # vLLM 需要现代版本的 PyTorch
transformers>=4.40.0  # 现代 transformer 库
numpy>=1.24.0,<2.0.0  # numpy 2.x 有破坏性变更

# Core MinerU
mineru[core]>=2.5.0

# vLLM Engine (for VLM-vLLM-Engine mode, GPU version only)
# Required for high-performance VLM inference
vllm>=0.10.1.1,<0.12

# Image Augmentation (Version Pinned for Compatibility)
albumentations>=1.4.4,<2.0.0
albucore>=0.0.13,<0.0.20

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# LitServe for GPU Load Balancing
litserve>=0.2.0

# Async HTTP Client
aiohttp>=3.11.0

# Logging
loguru>=0.7.0

# Office Document Parsing
# 注意: markitdown 依赖 markdownify, 必须先指定兼容版本
markdownify>=1.2.2
markitdown>=0.1.3

# MinIO Object Storage
minio>=7.2.0

# Optional: For better performance
# ujson>=5.10.0

